<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/receiver_text"
        android:text="SALAMsdas  sad asd ad a asdas sads sada  sa asd ad asdsa sadasd sad as d asdasds"
        android:maxWidth="190dp"
        android:minWidth="50dp"
        android:textSize="15sp"
        android:gravity="start"
        android:background="@drawable/receiver_shape"
        android:textColor="@color/black"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="40dp"
        app:layout_constraintTop_toTopOf="parent"
        />
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:id="@+id/sender_image1"
        app:layout_constraintRight_toLeftOf="@id/receiver_text"
        app:layout_constraintTop_toBottomOf="@id/receiver_text"
        android:scaleType="centerCrop"/>

</androidx.constraintlayout.widget.ConstraintLayout>